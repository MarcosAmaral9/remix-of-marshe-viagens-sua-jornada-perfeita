/**
 * Feature flags do site.
 *
 * SHOW_TRIP_INFO: quando `false`, oculta em toda a UI as menções de valores
 * (R$ ...), datas de partida/retorno e duração dos pacotes (dias/noites).
 * Os dados permanecem intactos no código para poderem ser reativados no futuro
 * simplesmente alterando este flag para `true`.
 */
export const SHOW_TRIP_INFO = false;
